<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Our Story | Reyto Florist</title>

    <link href="https://fonts.googleapis.com/css2?family=Cinzel:wght@400;700&family=Lato:wght@300;400;700&display=swap" rel="stylesheet">

    <style>
        :root {
            --primary-pink: #f8e1e7;
            --dark-rose: #5a3e46;
            --gold: #c5a059;
            --soft-white: #faf7f2;
        }

        body {
            margin: 0;
            font-family: 'Lato', sans-serif;
            background: var(--soft-white);
            color: var(--dark-rose);
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
        }

        .about-card {
            background: white;
            width: 90%;
            max-width: 800px;
            padding: 80px 60px;
            box-shadow: 0 30px 70px rgba(0,0,0,0.05);
            text-align: center;
            position: relative;
            animation: slideUp 1s ease;
        }

        .about-card::before {
            content: "🌸";
            font-size: 40px;
            position: absolute;
            top: -25px;
            left: 50%;
            transform: translateX(-50%);
            background: var(--soft-white);
            padding: 10px;
        }

        h1 {
            font-family: 'Cinzel', serif;
            font-size: 42px;
            letter-spacing: 6px;
            text-transform: uppercase;
            margin-bottom: 30px;
        }

        p {
            font-size: 18px;
            line-height: 2;
            font-weight: 300;
            color: #666;
            margin-bottom: 40px;
        }

        .philosophy-grid {
            display: grid;
            grid-template-columns: 1fr 1fr 1fr;
            gap: 20px;
            margin-bottom: 50px;
            border-top: 1px solid #eee;
            padding-top: 40px;
        }

        .philosophy-item h3 {
            font-family: 'Cinzel', serif;
            font-size: 14px;
            letter-spacing: 2px;
            margin-bottom: 10px;
        }

        .philosophy-item span {
            font-size: 12px;
            color: var(--gold);
            text-transform: uppercase;
        }

        .back-link {
            text-decoration: none;
            color: var(--dark-rose);
            font-size: 11px;
            text-transform: uppercase;
            letter-spacing: 3px;
            font-weight: 700;
            transition: 0.3s;
            border-bottom: 1px solid var(--gold);
            padding-bottom: 5px;
        }

        .back-link:hover {
            color: var(--gold);
        }

        @keyframes slideUp {
            from { opacity: 0; transform: translateY(40px); }
            to { opacity: 1; transform: translateY(0); }
        }

        @media (max-width: 600px) {
            .philosophy-grid { grid-template-columns: 1fr; }
            .about-card { padding: 50px 30px; }
        }
    </style>
</head>
<body>

    <div class="about-card">
        <h1>Our Story</h1>
        <p>
            Reyto Florist was born from a simple belief: that every petal holds a poem. Based in the heart of the city, we curate premium botanical experiences that transform ordinary spaces into extraordinary sanctuaries.
        </p>
        
        <div class="philosophy-grid">
            <div class="philosophy-item">
                <span>Quality</span>
                <h3>Premium</h3>
            </div>
            <div class="philosophy-item">
                <span>Care</span>
                <h3>Hand-Picked</h3>
            </div>
            <div class="philosophy-item">
                <span>Design</span>
                <h3>Bespoke</h3>
            </div>
        </div>

        <a href="index.html" class="back-link">← Return to Boutique</a>
    </div>

</body>
</html>